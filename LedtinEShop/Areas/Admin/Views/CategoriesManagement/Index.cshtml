@model IEnumerable<LedtinEShop.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="box">
    <div class="box-header">
        <h3 class="box-title">Data Table For Categories</h3>
    </div>
    <a class="btn" href="~/Admin/CategoriesManagement/Create">
        <i class="fa fa-plus-square"> Create New</i>
    </a>
    <!-- /.box-header -->
    <div class="box-body" style="overflow-x:auto">
        <table id="example1" class="table table-bordered table-striped table-hover">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Id)</th>
                    <th>@Html.DisplayNameFor(model => model.Name)</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            <a class="btn" href="~/Admin/CategoriesManagement/Edit/@item.Id">
                                <i class="fa fa-edit"> Edit</i>
                            </a>
                            <a class="btn" href="~/Admin/CategoriesManagement/Details/@item.Id">
                                <i class="fa fa-search"> Details</i>
                            </a>
                            <a class="btn" href="~/Admin/CategoriesManagement/Delete/@item.Id">
                                <i class="fa fa-ban"> Delete</i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
            <tfoot>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Id)</th>
                    <th>@Html.DisplayNameFor(model => model.Name)</th>
                    <th>Action</th>
                </tr>
            </tfoot>
        </table>
    </div>
    <!-- /.box-body -->
</div>